# 미완 작업 정리 (w.Lighter Django 백엔드)

> 자동 작업(오토파일럿) 1차 패스 결과. 작성: 2026-06-19
> 범위: 백엔드만. 프런트(css/js/html)는 타 팀 담당. covers·relationships는 확인만.
> 제약 준수: 파일 삭제·git push 안 함. DB는 SQLite 유지.

---

## A. 이번에 완료한 것 (코드 + 내부 테스트 통과)

- **환경**: 활성 conda `.venv`에 `Django 5.2.15`, `django-allauth`, `PyJWT`, `cryptography` 설치. `requirements.txt`에 누락분 추가. `python manage.py check` 0 issues, 전체 마이그레이션 적용, 단위테스트 **19개 통과**.
- **accounts**(신규 구축): `Profile`(nickname/oauth_provider/provider_user_id/withdrawn_at) 모델, 닉네임 검증(2~10자·한영숫자·공백불가), 소셜가입 시 Profile 생성 시그널, 마이페이지 조회/수정, **회원탈퇴(잔여 크레딧 소멸 + 비활성화) + 탈퇴 후 7일 재가입 차단 어댑터**. 테스트 9개.
- **translation**(연동 구축): `TranslationResult` 모델을 ERD에 맞춰 확장(원문 스냅샷, version_no, glossary_can/annotation_can/inspection_report JSON), **버전 관리**((회차×국가) 최대 3개·원문 미변경 시 새 버전 미생성), 검수 챗봇 뷰(현재 버전 갱신·새 버전 X·1000자 제한·대화 저장). 테스트 7개.
- **guides**(연동 구축): `LocalizationGuide.target_country` nullable화, 생성/목록/상세/삭제 뷰, **작품당 최대 5개 보관**, PDF 다운로드(요청 시 생성·미저장). 테스트 3개.
- **공통**: `common/wlighter_client.py` — FastAPI 번역 서비스 HTTP 클라이언트(country↔locale 매핑, 안전 실패).
- **settings 하드닝**: SECRET_KEY/DEBUG/ALLOWED_HOSTS/CSRF_TRUSTED_ORIGINS/WLIGHTER_API_BASE/OPENAI_API_KEY를 env 기반으로. DEBUG=False 시 HTTPS 보안 설정(SSL redirect, secure cookie, HSTS, nosniff) 적용. `.env.example` 추가.

---

## B. 외부 키/서버가 있어야 끝낼 수 있는 것 (코드는 작성됨, 라이브 검증 불가)

각 항목 = "무엇이 필요한지".

1. **소셜 로그인(구글/카카오/네이버) 실동작**
   - 필요: `.env`에 `GOOGLE/KAKAO/NAVER CLIENT_ID·SECRET` 입력.
   - 필요: allauth `SocialApp`을 admin 또는 fixture로 등록(SITE_ID=1). **`django.contrib.sites`가 INSTALLED_APPS에 없음** → allauth Site 연동 방식 확인 후 필요 시 추가.
   - 현재: 어댑터/재가입차단/Profile 생성 로직은 테스트 유저로 검증 완료. 실제 OAuth 왕복은 키 없이는 불가.

2. **토스 페이먼츠 결제/환불**
   - 필요: `.env`에 `TOSS_CLIENT_KEY·SECRET`, 실제 결제 위젯/콜백 도메인.
   - 현재: credits 앱 로직(잔액=거래합계, 차감 가드, 7일·미사용 환불 조건)은 존재. 실 PG 왕복 검증은 키 필요.

3. **OpenAI 기반 AI 기능**(캐릭터 추출, 표지 생성, 관계도 생성)
   - 필요: `.env`에 `OPENAI_API_KEY`.
   - 현재: 서비스 코드 존재(characters/covers/relationships). 라이브 호출 미검증.

4. **FastAPI 번역/현지화 서버 연동**
   - 필요: `WLIGHTER_API_BASE`에 `모델링 작업_통합 초안/api_server.py` 등으로 실제 서비스 기동.
   - 확인 필요: **`/api/guide` 응답 필드명** — 현재 클라이언트는 `guideContent/guide/content/...`를 방어적으로 탐색. 실제 응답 스키마 확인 후 매핑 고정 권장.
   - `/api/translate`, `/api/inspect-chat` 매핑은 `docs/translation_api_contract.md` 기준으로 작성. 서버 기동 후 E2E 스모크 필요.

5. **PDF 다운로드(가이드/관계도)**
   - 필요: `reportlab`(또는 weasyprint) 설치. 미설치 시 현재는 **텍스트 파일로 폴백**.
   - `pip install reportlab` 후 `guides/views.py:guide_download`가 자동으로 PDF 생성.

---

## C. 정보만 주어지면 코드로 더 할 수 있는 것 (외부 키 불필요)

1. **회원탈퇴 데이터 완전 삭제 + 7일 후 영구 삭제 스케줄러**
   - 현재: 비활성화 + withdrawn_at 기록 + 크레딧 소멸까지만. 요구사항의 "대화 내역 등 사용자 데이터 삭제, 식별자/이메일 7일 보관 후 삭제"의 **배치/cron(예: management command + 스케줄러)** 미구현.
2. **works / characters / credits 정밀 검증**
   - import/check는 통과. 다만 요구사항 세부(작품 제목 50자·시놉시스 10000자·목록 제목 30자 말줄임·캐릭터 20명 제한·역할 정렬 등)의 **정밀 단위테스트가 없음** → 추가 검증 권장.
3. **글로서리(GLOSSARY) 연결**
   - ERD의 `GLOSSARY` 테이블(work×country 용어집)이 Django 측 모델로는 미연결. translation 결과의 `glossary_can`만 저장됨.
   - 결정 필요: 용어집을 (a) FastAPI glossary API로 관리할지 (b) Django 모델로 둘지. `모델링 작업_통합 초안`에 단일 테이블 glossary 설계가 이미 있음(참고).

---

## D. covers / relationships (확인만 — 수정 안 함)

- `python manage.py check` 통과 → import/모델/URL 로딩 정상.
- 모델 변경 드리프트로 인한 미생성 마이그레이션은 없음(characters의 `relation` 필드 제거 마이그레이션 1건은 이번에 생성·적용).
- E2E 플로우(이미지/HTML 생성)는 OpenAI 키 + 외부 의존이라 라이브 미검증. 코드 자체 수정은 하지 않음(타 팀 담당).

---

## E. 운영 디테일 (배포 전 체크)

- `python manage.py check --deploy` 잔여 경고 2건:
  - `SECRET_KEY`: 운영용 긴 랜덤 키를 `.env`의 `DJANGO_SECRET_KEY`로 주입.
  - `SECURE_HSTS_PRELOAD`: 필요 시 settings에 추가(프리로드는 신중히).
- 운영 시 `.env`: `DJANGO_DEBUG=False`, `DJANGO_ALLOWED_HOSTS`, `DJANGO_CSRF_TRUSTED_ORIGINS` 설정.
- (선택) MySQL 전환: 이번엔 SQLite 유지. ERD는 MySQL 스타일이므로 추후 드라이버(`mysqlclient`/`PyMySQL`) + 접속정보 필요.
