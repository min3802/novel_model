# Core
requests>=2.31
python-dotenv>=1.0
openai>=1.0
numpy>=1.26
langgraph>=1.2,<2

# HTML parsing (bs4)
beautifulsoup4>=4.12

# Embedding / retrieval (KURE 임베딩 + Qdrant)
sentence-transformers>=3.0
qdrant-client>=1.7

# 한국어 문장 분리 (retriever sentence 전략, 미설치 시 paragraph 폴백)
kiwipiepy>=0.17
